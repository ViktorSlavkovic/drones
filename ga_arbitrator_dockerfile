FROM archlinux/base:latest
RUN pacman -Syq --noconfirm glibc

WORKDIR /app
COPY ./docker_data/* ./

ENTRYPOINT ["sh", "-c", "./main --problem_file=$PROBLEM_FILE --solver_type=ga --ga_is_standalone=false --ga_is_arbitrator --ga_evaluator_service_address=$EVALUATOR_SERVICE_HOST:$EVALUATOR_SERVICE_PORT" ]

